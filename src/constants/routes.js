export const PATHS = {
    ERROR: '/error',
    SIGN_IN: '/sign-in',
    REVOKE_TOKEN: '/revoke-token'
}

export const ALLOWED_ROUTES = [PATHS.SIGN_IN, PATHS.ERROR]

export default {
    PATHS,
    ALLOWED_ROUTES
}